% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_phyloseq.R
\name{abundance_taxid_to_phyloseq}
\alias{abundance_taxid_to_phyloseq}
\title{Import an abundance table keyed by NCBI taxid as phyloseq}
\usage{
abundance_taxid_to_phyloseq(
  counts,
  metadata = NULL,
  xml = NULL,
  trim_char = FALSE
)
}
\arguments{
\item{counts}{Matrix, data frame, or path to a TSV/CSV.}

\item{metadata}{Optional sample legend. QIIME 2 \code{sample-id} or a first-column
sample id, plus a \code{target} column when you have one.}

\item{xml}{Optional NCBI taxonomy XML. \code{NULL} fetches lineages from NCBI.}

\item{trim_char}{Split legend sample ids on this character before matching
them to column names.}
}
\value{
A \code{phyloseq} object, or an \code{archi_phyloseq} list.
}
\description{
Rows are taxids (row names, or a \code{taxid} / \code{taxonomy_id} / \code{taxon_id}
column). Other columns are samples. Lineages come from
\code{\link[=taxids_to_lineage]{taxids_to_lineage()}}; pass \code{xml} to stay offline. A rank-formula tree is
attached. Host and organelle taxids are dropped.
}
\examples{
counts <- system.file("extdata", "abundance-taxid-bee.tsv", package = "aRchiteutis")
xml <- paste(readLines(system.file("extdata", "ncbi_taxonomy.xml",
                                   package = "aRchiteutis"), warn = FALSE),
             collapse = "\n")
legend <- system.file("extdata", "legend.csv", package = "aRchiteutis")
ps <- abundance_taxid_to_phyloseq(counts, metadata = legend, xml = xml,
                                  trim_char = "_")
if (inherits(ps, "phyloseq")) phyloseq::ntaxa(ps) else nrow(ps$otu_table)

}
