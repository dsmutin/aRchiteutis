% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{archi_flag_samples}
\alias{archi_flag_samples}
\title{Flag samples that fail a read-count floor or a rarefaction curve}
\usage{
archi_flag_samples(
  df,
  min_reads = 1000,
  curve_gain = 0.15,
  curve_fraction = 0.8,
  curve_reps = 5L,
  seed = 123L
)
}
\arguments{
\item{df}{Tidy aRchiteutis table.}

\item{min_reads}{Minimum per-sample count sum. The harness floor is 1000.}

\item{curve_gain}{Maximum allowed terminal relative gain in observed richness.}

\item{curve_fraction}{Fraction of rank-table reads used for the terminal
rarefaction point.}

\item{curve_reps}{Rarefaction replicates used to estimate terminal richness.}

\item{seed}{RNG seed.}
}
\value{
A data frame with columns \code{sample}, \code{reads}, \code{reason}, \code{detail}.
Kept samples are not listed.
}
\description{
Read depth uses \code{profile_reads} from the importer when available; otherwise
it is the sum of counts at the imported rank. A sample under \code{min_reads} is
dropped. Remaining samples are rarefied on all taxa at \code{curve_fraction} of
their rank-table depth. If observed richness still rises by more than
\code{curve_gain} between that point and full depth, the curve has not levelled
off and the sample is dropped.
}
\keyword{internal}
