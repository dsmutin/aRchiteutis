% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{archi_flag_samples}
\alias{archi_flag_samples}
\title{Flag samples that fail a read-count floor or a rarefaction curve}
\usage{
archi_flag_samples(
  df,
  min_reads = 1000,
  curve_gain = 0.5,
  top = 40L,
  seed = 123L
)
}
\arguments{
\item{df}{Tidy aRchiteutis table.}

\item{min_reads}{Minimum per-sample count sum. The harness floor is 1000.}

\item{curve_gain}{Maximum allowed relative gain in observed richness.}

\item{top}{Taxa used for the curve check.}

\item{seed}{RNG seed.}
}
\value{
A data frame with columns \code{sample}, \code{reads}, \code{reason}, \code{detail}.
Kept samples are not listed.
}
\description{
Read depth is the sum of counts in the tidy table (one rank, so clade reads
are not added twice). A sample under \code{min_reads} is dropped. Remaining
samples are rarefied, on the \code{top} taxa, at half their depth and at full
depth. If observed richness still rises by more than \code{curve_gain}, the
curve has not levelled off and the sample is dropped.
}
\keyword{internal}
