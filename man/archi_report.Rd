% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{archi_report}
\alias{archi_report}
\title{Preliminary MultiQC report for a taxonomic profile}
\usage{
archi_report(
  path,
  legend,
  outdir,
  pattern = "",
  trim_char = FALSE,
  rank = "G",
  source = c("auto", "kraken", "kaiju", "qza", "abundance"),
  target = "target",
  min_reads = 1000,
  curve_gain = 0.15,
  curve_fraction = 0.8,
  curve_reps = 5L,
  plots = c("composition", "donut", "alpha", "beta", "rarefaction"),
  beta_method = "bray",
  order_samples = c("fpc", "hclust", "abundance", "alpha", "none"),
  style = c("box", "violin", "raincloud"),
  top = 15L,
  rarefaction_depths = NULL,
  rarefaction_reps = 1L,
  contrast = NULL,
  strict = TRUE,
  counts = NULL,
  xml = NULL
)
}
\arguments{
\item{path}{Directory of Kraken-family reports, Kaiju output, or QIIME 2
artifacts. Ignored for \code{source = "abundance"} except as a label.}

\item{legend}{QIIME 2 manifest. Sample ids may be \code{sample-id}, \code{sample_id},
\code{SampleID}, \code{sample}, or the first column. \code{target} names the grouping
column (\code{"target"}, or for the bundled legend \code{"stage"}).}

\item{outdir}{Directory to create. Existing files with the same names are
overwritten.}

\item{pattern}{File filter for Kraken or Kaiju directories.}

\item{trim_char}{Passed to the importer so file names match legend ids.}

\item{rank}{Rank code for Kraken-family reports.}

\item{source}{One of \code{"auto"}, \code{"kraken"}, \code{"kaiju"}, \code{"qza"}, \code{"abundance"}.}

\item{target}{Column in \code{legend} copied to \code{target} for grouped plots.}

\item{min_reads}{Samples below this count sum are dropped (\code{read_count}).}

\item{curve_gain}{Samples whose observed richness rises by more than this
fraction between \code{curve_fraction} depth and full depth are dropped
(\code{diversity_curve}).}

\item{curve_fraction}{Terminal rarefaction fraction used by the curve check.}

\item{curve_reps}{Replicates used by the curve check.}

\item{plots}{Character vector of plot ids. Any of \code{composition}, \code{donut},
\code{barplot}, \code{alpha}, \code{beta}, \code{rarefaction}, \code{heattree}, \code{upset}, \code{difftree}.}

\item{beta_method}{Name passed to \code{\link[=df2beta]{df2beta()}}, including phyloseq distances
and \code{"aitchison"} when the matching package is installed.}

\item{order_samples}{Sample order for \code{\link[=df2composition]{df2composition()}}.}

\item{style}{\code{"box"}, \code{"violin"} or \code{"raincloud"} for alpha and bar plots.}

\item{top}{Taxa kept before composition, rarefaction and heat-tree plots.}

\item{rarefaction_depths}{Depths for \code{\link[=df2rarefaction]{df2rarefaction()}}. \code{NULL} uses a short
grid capped at the smallest kept library.}

\item{rarefaction_reps}{Replicates per rarefaction depth.}

\item{contrast}{Optional two target levels for \code{difftree}. Required when the
target column has more than two levels.}

\item{strict}{Stop if a requested plot fails or does not return a ggplot.
Set \code{FALSE} to keep the error as a note in the standalone HTML.}

\item{counts}{Abundance table for \code{source = "abundance"}.}

\item{xml}{NCBI taxonomy XML for the abundance importer. \code{NULL} fetches.}
}
\value{
Invisibly, a list with \code{dropped}, \code{html}, \code{outdir} and \code{plots}.
}
\description{
Reads every profile the chosen importer understands, builds a phyloseq
object with a taxonomy tree, and drops samples that fall below \code{min_reads}
or whose rarefaction curve is still rising. Dropped samples are written to
\code{dropped_samples.csv} and shown as a table. Each requested \verb{df2*} plot is
saved with a one-line method caption as MultiQC custom content,
and the same panels are collected into \code{architeutis_report.html}.
}
\details{
The HTML and the MultiQC section both end with the statement that this is
a preliminary report only, not a final analysis.
}
\examples{
out <- tempfile("archi-report")
path <- system.file("extdata", package = "aRchiteutis")
legend <- system.file("extdata", "legend.csv", package = "aRchiteutis")
archi_report(
  path, legend, outdir = out, pattern = "m1[12]_", trim_char = "_",
  target = "stage", plots = c("composition", "rarefaction"),
  rarefaction_depths = c(500L, 2000L), rarefaction_reps = 1L, top = 8L
)

}
