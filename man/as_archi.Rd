% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_table.R
\name{as_archi}
\alias{as_archi}
\title{Coerce a table into the canonical aRchiteutis long format}
\usage{
as_archi(df, clade = "S")
}
\arguments{
\item{df}{A data frame that already looks like a \code{\link{get_counts}} result, or
that uses common aliases (\code{taxon}/\code{otu}/\code{asv} for \code{taxa}, \code{rank} for
\code{clade}, \code{count}/\code{reads} for \code{N}, and so on).}

\item{clade}{Character. Rank letter to fill in when \code{clade} is missing.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} in canonical aRchiteutis shape.
}
\description{
The plotting helpers all expect a tidy tibble with columns \code{taxa}, \code{clade},
\code{sample}, \code{N} (counts), \code{amount} (fraction of all reads) and \code{amount_cl}
(fraction of classified reads), plus optional legend columns.
}
