% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_diversity.R
\name{beta_dist}
\alias{beta_dist}
\title{Sample-by-sample beta-diversity distance dispatcher}
\usage{
beta_dist(
  samples_by_taxa,
  method = "bray",
  dist_function = NULL,
  adiv_index = "jaccard",
  zero_replace = "cmultRepl",
  pseudocount = 0.65
)
}
\arguments{
\item{samples_by_taxa}{Numeric matrix with samples in rows and taxa in
columns (i.e. the transpose of \code{\link[=df_untidy]{df_untidy()}} output).}

\item{method}{Backend: \code{"bray"} (or any \pkg{abdiv} \code{dist_function}),
\code{"aitchison"} (compositional / Aitchison distance via
\code{\link[robCompositions:aDist]{robCompositions::aDist()}}) or \code{"adiv"} (Jaccard-family dissimilarity via
\code{\link[adiv:Jac]{adiv::Jac()}}).}

\item{dist_function}{Optional distance function from \pkg{abdiv}. When
supplied it always wins (backward-compatible path via
\code{\link[usedist:dist_make]{usedist::dist_make()}}), regardless of \code{method}.}

\item{adiv_index}{For \code{method = "adiv"}, one of \code{"jaccard"} (total),
\code{"turnover"} (replacement) or \code{"richness"} (richness difference).}

\item{zero_replace}{For \code{method = "aitchison"}, \code{"cmultRepl"} (Bayesian
multiplicative replacement via \code{\link[zCompositions:cmultRepl]{zCompositions::cmultRepl()}}) or a numeric
pseudocount used as a simple multiplicative fallback.}

\item{pseudocount}{Numeric pseudocount used when zero replacement falls back
to (or is set to) a simple multiplicative replacement.}
}
\value{
A \link[stats:dist]{stats::dist} object of sample-by-sample distances.
}
\description{
Internal-ish dispatcher shared by the \verb{df2beta*} functions. Takes a
\strong{samples x taxa} matrix and returns a sample-by-sample \link[stats:dist]{stats::dist}
using the requested backend.
}
\keyword{internal}
