% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_diversity.R
\name{df2alpha_summary}
\alias{df2alpha_summary}
\title{Summary of many alpha-diversity metrics per sample}
\usage{
df2alpha_summary(df, split_by = FALSE, add_legend = FALSE, ...)
}
\arguments{
\item{df}{A tidy \code{tibble} from \code{\link[=get_counts]{get_counts()}}. Retain species-level rows for
meaningful diversity estimates.}

\item{split_by}{Integer column index (or \code{FALSE}) used to split the samples
into groups on the x-axis.}

\item{add_legend}{Integer column index/indices (or \code{FALSE}) whose values are
combined into the colour legend.}

\item{...}{Passed to the geoms.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Computes a broad panel of alpha-diversity indices (from the \pkg{abdiv}
package) for every sample and displays them as faceted violin + jitter plots.
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
legend <- system.file("extdata", "legend.csv", package = "aRchiteutis")
df <- get_counts(path = path, pattern = "m1[124]_", legend = legend,
                 trim_char = "_")
df2alpha_summary(df[df$clade == "S", ])

}
