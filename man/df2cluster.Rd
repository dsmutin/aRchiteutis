% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_ordination.R
\name{df2cluster}
\alias{df2cluster}
\title{Hierarchical clustering dendrogram}
\usage{
df2cluster(df, k_means = 2, use = "sp")
}
\arguments{
\item{df}{A numeric taxa-by-sample matrix, e.g. from \code{\link[=df_untidy]{df_untidy()}}. A
\link[phyloseq:phyloseq]{phyloseq::phyloseq} object or a long \code{df} from \code{\link[=get_counts]{get_counts()}} is also
accepted and reduced to a genus-level matrix first (a plain matrix is used
as-is).}

\item{k_means}{Integer. Number of clusters to outline with rectangles
(\verb{< 2} draws no rectangles).}

\item{use}{\code{"sp"} to cluster taxa (rows) or \code{"sample"} to cluster columns.}
}
\value{
Invisibly \code{NULL}; a dendrogram is drawn as a side effect.
}
\description{
Scales the matrix and draws a Ward.D2 dendrogram of either the taxa
(\code{use = "sp"}) or the samples (\code{use = "sample"}).
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
df <- get_counts(path = path, pattern = "m[13][124]_", trim_char = "_")
mat <- df_untidy(df, clade = "G", top = 20, scale = "log2")
grDevices::pdf(tempfile()); df2cluster(mat, k_means = 4, use = "sp")
grDevices::dev.off()

}
