% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{df2ggraph}
\alias{df2ggraph}
\title{Visualise a tidygraph network with ggraph}
\usage{
df2ggraph(
  x,
  layout = "fr",
  size_by = "degree",
  label = TRUE,
  label_size = 3,
  node_alpha = 0.9,
  ...
)
}
\arguments{
\item{x}{A \link[tidygraph:tbl_graph]{tidygraph::tbl_graph} (e.g. from \code{\link[=df2graph]{df2graph()}}), or a \code{df} /
\link[phyloseq:phyloseq]{phyloseq::phyloseq} object / matrix, in which case \code{\link[=df2graph]{df2graph()}} is run
first.}

\item{layout}{A \pkg{ggraph} layout name (default \code{"fr"}, Fruchterman-Reingold).}

\item{size_by}{Node column mapped to point size (default \code{"degree"}).}

\item{label}{Logical. Draw taxon labels.}

\item{label_size}{Numeric. Label text size.}

\item{node_alpha}{Numeric. Node point alpha.}

\item{...}{Passed to \code{\link[=df2graph]{df2graph()}} when \code{x} is not already a \code{tbl_graph}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object (a \pkg{ggraph} plot).
}
\description{
Draws a \link[tidygraph:tbl_graph]{tidygraph::tbl_graph} with \pkg{ggraph}: edges coloured by the
signed correlation (red = negative, blue = positive) with alpha scaled by
magnitude, nodes coloured by community (viridis) and sized by centrality, and
taxon labels in italic. This is the VIZ half of the engine; when given a
\code{df} / phyloseq / matrix it calls \code{\link[=df2graph]{df2graph()}} first.
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
df <- get_counts(path = path, pattern = "m[13][124]_", trim_char = "_")
mat <- df_untidy(df, clade = "G", top = 12, scale = "scale")
set.seed(1)
df2ggraph(mat, threshold = 0.3)

}
\seealso{
\code{\link[=df2graph]{df2graph()}} for the graph construction.
}
