% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{df2graph}
\alias{df2graph}
\title{Build a tidygraph co-occurrence network from taxa}
\usage{
df2graph(
  df,
  clade = "G",
  cor_method = "pearson",
  threshold = 0.3,
  cluster_method = "louvain",
  top = FALSE,
  ...
)
}
\arguments{
\item{df}{A numeric taxa-by-sample matrix, e.g. from \code{\link[=df_untidy]{df_untidy()}}. A
\link[phyloseq:phyloseq]{phyloseq::phyloseq} object or a long \code{df} from \code{\link[=get_counts]{get_counts()}} is also
accepted and reduced to a matrix first with \code{\link[=df_untidy]{df_untidy()}} (a plain matrix
is used as-is).}

\item{clade}{Character. Clade letter used when reducing a \code{df} / phyloseq
object to a matrix (default \code{"G"}, genus).}

\item{cor_method}{Correlation method, one of \code{"pearson"}, \code{"spearman"} or
\code{"kendall"} (passed to \link[stats:cor]{stats::cor}).}

\item{threshold}{Numeric in \verb{[0, 1]}. Keep only edges with
\code{abs(correlation) >= threshold}.}

\item{cluster_method}{Community-detection method: one of \code{"louvain"},
\code{"walktrap"}, \code{"infomap"}, \code{"label_prop"}, \code{"fast_greedy"},
\code{"leading_eigen"}, \code{"edge_betweenness"} or \code{"components"}.}

\item{top}{Integer or \code{FALSE}. Keep only the \code{top} most abundant taxa when
reducing a \code{df} / phyloseq object (ignored for a bare matrix).}

\item{...}{Passed to \code{\link[=as_samovar_matrix]{as_samovar_matrix()}}.}
}
\value{
A \link[tidygraph:tbl_graph]{tidygraph::tbl_graph} with the nodes context active.
}
\description{
Computes a taxon-by-taxon correlation matrix, keeps edges whose absolute
correlation is at least \code{threshold}, and returns a
\link[tidygraph:tbl_graph]{tidygraph::tbl_graph}. Nodes are annotated with weighted centralities
(degree, betweenness, closeness) and community membership; edges carry the
signed correlation (\code{weight}), its magnitude (\code{abs_weight}) and \code{sign}.
}
\details{
This is the CALC half of the tidygraph / ggraph engine; pass the result to
\code{\link[=df2ggraph]{df2ggraph()}} to draw it. \code{\link[=df2chord]{df2chord()}} remains the separate circular chord
view.
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
df <- get_counts(path = path, pattern = "m[13][124]_", trim_char = "_")
mat <- df_untidy(df, clade = "G", top = 12, scale = "scale")
set.seed(1)
g <- df2graph(mat, threshold = 0.3)
g

}
\seealso{
\code{\link[=df2ggraph]{df2ggraph()}} to visualise the graph, \code{\link[=df2chord]{df2chord()}} for the chord
view.
}
