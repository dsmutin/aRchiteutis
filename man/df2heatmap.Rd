% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_ordination.R
\name{df2heatmap}
\alias{df2heatmap}
\title{Base-R heatmap of a taxa-by-sample matrix}
\usage{
df2heatmap(df, clade = FALSE, trim = FALSE, ...)
}
\arguments{
\item{df}{A numeric taxa-by-sample matrix, e.g. from \code{\link[=df_untidy]{df_untidy()}}. A
\link[phyloseq:phyloseq]{phyloseq::phyloseq} object or a long \code{df} from \code{\link[=get_counts]{get_counts()}} is also
accepted and reduced to a genus-level matrix first (a plain matrix is used
as-is).}

\item{clade}{Ignored; kept for backward compatibility (do the clade / trim
selection in \code{\link[=df_untidy]{df_untidy()}} instead).}

\item{trim}{Ignored; kept for backward compatibility.}

\item{...}{Passed to \link[stats:heatmap]{stats::heatmap}.}
}
\value{
Invisibly, the result of \link[stats:heatmap]{stats::heatmap} (drawn as a side effect).
}
\description{
Base-R heatmap of a taxa-by-sample matrix
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
df <- get_counts(path = path, pattern = "m[13][124]_", trim_char = "_")
mat <- df_untidy(df, clade = "G", top = 10)
grDevices::pdf(tempfile()); df2heatmap(mat, scale = "row", Colv = NA)
grDevices::dev.off()

}
