% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netcomi.R
\name{df2netcomi}
\alias{df2netcomi}
\title{Build and analyse a microbial network with NetCoMi}
\usage{
df2netcomi(
  df,
  clade = "G",
  measure = "pearson",
  top = FALSE,
  sparsMethod = "threshold",
  thresh = 0.3,
  verbose = 0,
  seed = 42,
  netConstruct_args = list(),
  netAnalyze_args = list(),
  ...
)
}
\arguments{
\item{df}{A numeric taxa-by-sample matrix, e.g. from \code{\link[=df_untidy]{df_untidy()}}. A
\link[phyloseq:phyloseq]{phyloseq::phyloseq} object or a long \code{df} from \code{\link[=get_counts]{get_counts()}} is also
accepted and reduced to a matrix first (a plain matrix is used as-is).}

\item{clade}{Character. Clade letter used when reducing a \code{df} / phyloseq
object to a matrix (default \code{"G"}, genus).}

\item{measure}{Association measure passed to \code{NetCoMi::netConstruct()}
(default \code{"pearson"}).}

\item{top}{Integer or \code{FALSE}. Keep only the \code{top} most abundant taxa when
reducing a \code{df} / phyloseq object.}

\item{sparsMethod}{Sparsification method for \code{NetCoMi::netConstruct()}
(default \code{"threshold"}).}

\item{thresh}{Numeric association threshold used when
\code{sparsMethod = "threshold"} (default \code{0.3}).}

\item{verbose}{Verbosity level forwarded to NetCoMi (default \code{0}).}

\item{seed}{Integer random seed for reproducibility (default \code{42}).}

\item{netConstruct_args, netAnalyze_args}{Optional named lists of extra
arguments forwarded to \code{NetCoMi::netConstruct()} / \code{NetCoMi::netAnalyze()}.
\code{netAnalyze()} is called with \code{graphlet = FALSE} by default (faster, fewer
optional dependencies); pass \code{netAnalyze_args = list(graphlet = TRUE)} to
enable the graphlet-correlation analysis.}

\item{...}{Passed to the internal matrix coercion (see \code{\link[=df_untidy]{df_untidy()}}).}
}
\value{
A list with elements \code{net} (the \code{microNet} object from
\code{netConstruct()}) and \code{props} (the \code{microNetProps} object from
\code{netAnalyze()}).
}
\description{
Constructs a taxon-by-taxon association network with
\code{NetCoMi::netConstruct()} and analyses it with \code{NetCoMi::netAnalyze()},
returning both objects so networks can be inspected or compared. NetCoMi is
an optional dependency: this function errors with an install hint when it is
not available.
}
\details{
Correlation-based measures (\code{"pearson"}, \code{"spearman"}) are recommended as
they need no extra compiled dependencies; measures such as \code{"spieceasi"} /
\code{"spring"} additionally require the \code{SpiecEasi} / \code{SPRING} packages.
}
\examples{
\dontrun{
path <- system.file("extdata", package = "aRchiteutis")
df <- get_counts(path = path, pattern = "m[13][124]_", trim_char = "_")
mat <- df_untidy(df, clade = "G", top = 12)
res <- df2netcomi(mat, measure = "pearson")
res$props
}

}
\seealso{
\code{\link[=df2netcomi_graph]{df2netcomi_graph()}} to bridge the result into the \code{\link[=df2ggraph]{df2ggraph()}}
engine.
}
