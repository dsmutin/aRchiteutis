% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netcomi.R
\name{df2netcomi_graph}
\alias{df2netcomi_graph}
\title{Bridge a NetCoMi network into a tidygraph / ggraph visualisation}
\usage{
df2netcomi_graph(
  x,
  threshold = 0.3,
  cluster_method = "louvain",
  plot = FALSE,
  ...
)
}
\arguments{
\item{x}{One of: the list returned by \code{\link[=df2netcomi]{df2netcomi()}}; a NetCoMi \code{microNet}
object; a \code{df} / \link[phyloseq:phyloseq]{phyloseq::phyloseq} object / matrix (in which case
\code{\link[=df2netcomi]{df2netcomi()}} is run first); or a \code{tbl_graph} (returned as-is).}

\item{threshold}{Numeric in \verb{[0, 1]}. Absolute-association edge threshold
applied when building the graph.}

\item{cluster_method}{Community-detection method (see \code{\link[=df2graph]{df2graph()}}).}

\item{plot}{Logical. When \code{TRUE}, return a \code{\link[=df2ggraph]{df2ggraph()}} plot; when \code{FALSE}
(default) return the annotated \link[tidygraph:tbl_graph]{tidygraph::tbl_graph}.}

\item{...}{Passed to \code{\link[=df2netcomi]{df2netcomi()}} (when \code{x} is raw data) and to
\code{\link[=df2ggraph]{df2ggraph()}} (when \code{plot = TRUE}).}
}
\value{
A \link[tidygraph:tbl_graph]{tidygraph::tbl_graph} (when \code{plot = FALSE}) or a
\link[ggplot2:ggplot]{ggplot2::ggplot} (when \code{plot = TRUE}).
}
\description{
Extracts the association matrix from a NetCoMi network and feeds it through
the same tidygraph engine as \code{\link[=df2graph]{df2graph()}}, so a NetCoMi-constructed network
can be drawn with \code{\link[=df2ggraph]{df2ggraph()}} for a look consistent with the native path.
}
\examples{
\dontrun{
path <- system.file("extdata", package = "aRchiteutis")
df <- get_counts(path = path, pattern = "m[13][124]_", trim_char = "_")
mat <- df_untidy(df, clade = "G", top = 12)
res <- df2netcomi(mat, measure = "pearson")
g <- df2netcomi_graph(res)          # a tbl_graph
df2netcomi_graph(res, plot = TRUE)  # a ggraph plot
}

}
\seealso{
\code{\link[=df2netcomi]{df2netcomi()}}, \code{\link[=df2graph]{df2graph()}}, \code{\link[=df2ggraph]{df2ggraph()}}.
}
