% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_ordination.R
\name{df2pca_sample}
\alias{df2pca_sample}
\title{PCA of samples}
\usage{
df2pca_sample(df, scale = TRUE, detect = FALSE, detect2 = "other", ...)
}
\arguments{
\item{df}{A numeric taxa-by-sample matrix, e.g. from \code{\link[=df_untidy]{df_untidy()}}. A
\link[phyloseq:phyloseq]{phyloseq::phyloseq} object or a long \code{df} from \code{\link[=get_counts]{get_counts()}} is also
accepted and is reduced to a genus-level matrix with a default
\code{\link[=df_untidy]{df_untidy()}} call first (a plain matrix is used as-is).}

\item{scale}{Logical. Scale variables to unit variance before PCA.}

\item{detect}{Character or \code{FALSE}. If set, samples whose column name matches
this pattern are coloured with \code{detect}, the rest with \code{detect2}.}

\item{detect2}{Character. Colour label for non-matching samples.}

\item{...}{Passed to \link[factoextra:fviz_pca_ind]{factoextra::fviz_pca_ind}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Runs a principal component analysis treating samples as observations and taxa
as variables, and draws the individuals plot with \pkg{factoextra}.
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
df <- get_counts(path = path, pattern = "m[13][124]_", trim_char = "_")
mat <- df_untidy(df, clade = "S", scale = "scale", keep_sample_name = FALSE)
df2pca_sample(mat, scale = FALSE)

}
