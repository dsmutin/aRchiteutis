% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phyloseq.R
\name{df2ps}
\alias{df2ps}
\title{Convert the aRchiteutis long count table into a phyloseq object}
\usage{
df2ps(df, clade = FALSE, ...)
}
\arguments{
\item{df}{A tidy \code{tibble} from \code{\link[=get_counts]{get_counts()}} (with the full clade hierarchy).}

\item{clade}{Character or \code{FALSE}. Clade letter to use as the operational
taxa (e.g. \code{"G"}). When \code{FALSE} (default) the finest clade present is used.}

\item{...}{Ignored, for forward compatibility.}
}
\value{
A \link[phyloseq:phyloseq]{phyloseq::phyloseq} object.
}
\description{
Builds a \link[phyloseq:phyloseq]{phyloseq::phyloseq} object from the long \code{tibble} produced by
\code{\link[=get_counts]{get_counts()}}. The finest clade present (or the one requested via \code{clade})
defines the operational taxa; the parent lineage is recovered with
\code{\link[=df_get_parents]{df_get_parents()}} and written to the \code{tax_table}, the per-sample counts (\code{N})
become the \code{otu_table}, and any legend columns (7+) become the \code{sample_data}.
}
\details{
A round-trip on genus-level counts (\code{ps2df(df2ps(df))}) preserves the genus
read counts and is a convenient correctness check.
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
df <- get_counts(path = path, pattern = "m1[124]_", trim_char = "_")
if (requireNamespace("phyloseq", quietly = TRUE)) {
  ps <- df2ps(df)
  ps
}

}
\seealso{
\code{\link[=ps2df]{ps2df()}} for the reverse conversion.
}
