% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_harness.R
\name{df2rarefaction}
\alias{df2rarefaction}
\title{Alpha-diversity rarefaction curves}
\usage{
df2rarefaction(
  df,
  split_by = FALSE,
  measures = c("Observed", "Shannon", "Simpson"),
  depths = NULL,
  n_reps = 3L,
  seed = 123L,
  top = FALSE
)
}
\arguments{
\item{df}{A tidy table from \code{\link[=get_counts]{get_counts()}}. Species rows are appropriate.}

\item{split_by}{Integer column index or column name used to colour samples.
\code{FALSE} colours by sample.}

\item{measures}{Character vector: any of \code{"Observed"}, \code{"Shannon"},
\code{"Simpson"}.}

\item{depths}{Integer depths. \code{NULL} uses a short grid up to the smallest library.}

\item{n_reps}{Integer. Rarefaction replicates per depth.}

\item{seed}{Integer. RNG seed.}

\item{top}{Integer or \code{FALSE}. Keep the \code{top} most abundant taxa before
rarefying (faster on full Kraken2 reports).}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object. The long replicate table is attached as
attribute \code{"rarefaction"}.
}
\description{
At each depth and replicate, each sample with enough reads is rarefied and
Observed / Shannon / Simpson are recorded. The plot follows the harness
grazing figure: one thin line per sample, a loess smooth by group, and a
dashed line at the depths that still include every plotted sample.
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
legend <- system.file("extdata", "legend.csv", package = "aRchiteutis")
df <- get_counts(path, pattern = "m1[12]_", legend = legend, trim_char = "_")
df2rarefaction(df[df$clade == "S", ], split_by = "stage",
               depths = c(20L, 50L), n_reps = 2L, top = 25L)

}
