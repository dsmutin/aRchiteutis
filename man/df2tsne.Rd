% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_ordination.R
\name{df2tsne}
\alias{df2tsne}
\title{t-SNE ordination of taxa}
\usage{
df2tsne(
  df,
  color = "clust",
  k_means = 10,
  text_top = FALSE,
  perplexity = 30,
  max_iter = 1000
)
}
\arguments{
\item{df}{A numeric taxa-by-sample matrix, e.g. from \code{\link[=df_untidy]{df_untidy()}}. A
\link[phyloseq:phyloseq]{phyloseq::phyloseq} object or a long \code{df} from \code{\link[=get_counts]{get_counts()}} is also
accepted and reduced to a top genus-level matrix first (a plain matrix is
used as-is).}

\item{color}{Either \code{"clust"} (default; colour by hierarchical cluster) or a
vector of length \code{nrow(df)} giving a grouping per taxon.}

\item{k_means}{Integer. Number of clusters when \code{color = "clust"}.}

\item{text_top}{Integer or \code{FALSE}. Label only the \code{text_top} most abundant
taxa (\code{-1} disables labels entirely).}

\item{perplexity}{Numeric. t-SNE perplexity (kept small speeds things up).}

\item{max_iter}{Integer. Maximum t-SNE iterations.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Runs t-SNE (via \pkg{tsne}) on a taxa-by-sample matrix and plots the taxa in
two dimensions, coloured by hierarchical cluster.
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
df <- get_counts(path = path, pattern = "m[13][124]_", trim_char = "_")
mat <- df_untidy(df, clade = "G", top = 30, scale = "scale")
set.seed(1)
df2tsne(mat, k_means = 4, text_top = 5, perplexity = 5, max_iter = 100)

}
