% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/df_transforms.R
\name{df_untidy}
\alias{df_untidy}
\title{Reshape a tidy count table into a taxa-by-sample matrix}
\usage{
df_untidy(
  df,
  clade = FALSE,
  amount_from = "amount",
  top = FALSE,
  trim = FALSE,
  scale = FALSE,
  drop_unclassified = FALSE,
  keep_sample_name = TRUE
)
}
\arguments{
\item{df}{A tidy \code{tibble} from \code{\link[=get_counts]{get_counts()}}.}

\item{clade}{Character or \code{FALSE}. Restrict to a single clade (e.g. \code{"G"}).}

\item{amount_from}{Character. Which value column to spread; one of
\code{"amount"} (default), \code{"N"} or \code{"amount_cl"}.}

\item{top}{Integer or \code{FALSE}. Keep only the \code{top} most abundant taxa.}

\item{trim}{Integer or \code{FALSE}. Deprecated alias for \code{top}; used when \code{top}
is \code{FALSE} so that legacy callers keep working.}

\item{scale}{\code{FALSE}, \code{"log2"} or \code{"scale"}. Optional per-column transform.}

\item{drop_unclassified}{Logical. Drop unclassified taxa first.}

\item{keep_sample_name}{Logical. If \code{FALSE}, column names encode the legend
columns (joined with \code{"_"}) instead of the bare sample name.}
}
\value{
A numeric matrix (taxa in rows, samples in columns).
}
\description{
Pivots the long table produced by \code{\link[=get_counts]{get_counts()}} into a numeric matrix with
taxa in the rows and samples in the columns, ready for base-graphics
heatmaps, clustering, PCA and other matrix-oriented plots.
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
df <- get_counts(path = path, pattern = "m1[124]_", trim_char = "_")
mat <- df_untidy(df, clade = "G", top = 5)
dim(mat)

}
