% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_table.R
\name{from_abundance}
\alias{from_abundance}
\title{Import a taxa-by-sample abundance table}
\usage{
from_abundance(x, taxa = NULL, samples = NULL, clade = "S", legend = NULL)
}
\arguments{
\item{x}{Matrix, data frame or tibble of abundances.}

\item{taxa}{Optional character vector of taxon names (used when \code{x} is a
bare matrix without row names). Ignored when \code{x} already has a taxa
column / row names.}

\item{samples}{Optional character vector of sample names for the columns of
a wide table.}

\item{clade}{Character. Rank letter stored in the \code{clade} column (\code{"S"} by
default). A vector is recycled to the taxa.}

\item{legend}{Optional data frame or CSV path of sample metadata. Must
contain a \code{sample} column (or use the first column as the sample key).}
}
\value{
A \link[tibble:tibble]{tibble::tibble} in canonical aRchiteutis shape.
}
\description{
Accepts a wide numeric matrix / data frame (taxa in rows, samples in columns),
a wide data frame whose first column holds taxon names, or a long data frame
that already has \code{taxa} + \code{sample} (passed through \code{\link{as_archi}}).
}
\examples{
mat <- matrix(c(10, 5, 1, 20, 0, 4), nrow = 2,
              dimnames = list(c("Lactobacillus", "Bifidobacterium"),
                              c("s1", "s2", "s3")))
df <- from_abundance(mat, clade = "G")
df2composition(df)
}
