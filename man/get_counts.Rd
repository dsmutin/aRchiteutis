% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_counts.R
\name{get_counts}
\alias{get_counts}
\title{Read Kraken2 / Kaiju / Bracken reports into a tidy count table}
\usage{
get_counts(
  path,
  keep_unclassified = TRUE,
  pattern = "",
  trim_char = FALSE,
  output_type = "kraken2",
  legend = FALSE
)
}
\arguments{
\item{path}{Character. Path to the directory holding the report files.}

\item{keep_unclassified}{Logical. If \code{TRUE} (default), reads counted as
unclassified below a rank are kept as new \code{"<taxon> unclassified"} rows.}

\item{pattern}{Character. Optional regular expression used to select which
files in \code{path} to read.}

\item{trim_char}{Character or \code{FALSE}. Optional delimiter used to split each
file name; the first field becomes the sample name.}

\item{output_type}{Character. Type of classifier output, one of
\code{"kraken2"} (default), \code{"kaiju"} or \code{"bracken"}.}

\item{legend}{Character or \code{FALSE}. Optional path to a CSV legend whose first
column matches the (possibly trimmed) sample name; remaining columns are
joined onto the table.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with columns \code{taxa}, \code{clade}, \code{sample}, \code{N}
(number of reads), \code{amount} (fraction of all reads), \code{amount_cl} (fraction
of classified reads) and any additional legend columns.
}
\description{
Reads every classifier report in a directory and assembles a single long
(tidy) \code{tibble} with one row per taxon per sample. An optional legend file
can be joined onto the samples.
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
legend <- system.file("extdata", "legend.csv", package = "aRchiteutis")
df <- get_counts(path = path, pattern = "m1[12]_", legend = legend,
                 trim_char = "_")
head(df)

}
