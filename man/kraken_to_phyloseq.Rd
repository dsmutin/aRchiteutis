% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_phyloseq.R
\name{kraken_to_phyloseq}
\alias{kraken_to_phyloseq}
\title{Import Kraken, Kraken2, KrakenUniq or Bracken reports as phyloseq}
\usage{
kraken_to_phyloseq(
  path,
  pattern = "",
  rank = "G",
  legend = NULL,
  trim_char = FALSE
)
}
\arguments{
\item{path}{Directory of reports, or a character vector of files.}

\item{pattern}{Regular expression selecting files when \code{path} is a directory.}

\item{rank}{Rank code kept in the OTU table (\code{"G"} or \code{"S"}).}

\item{legend}{Path to a CSV legend. A QIIME2 manifest (\code{sample-id}) or the
package legend (sample id in the first column) are both accepted.}

\item{trim_char}{Passed through when matching legend ids to file names.
File names are trimmed the same way before they become sample ids.}
}
\value{
A \code{phyloseq} object, or an \code{archi_phyloseq} list with
\code{otu_table}, \code{tax_table}, \code{sample_data} and \code{phy_tree} when \pkg{phyloseq}
is not installed.
}
\description{
Import Kraken, Kraken2, KrakenUniq or Bracken reports as phyloseq
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
legend <- system.file("extdata", "legend.csv", package = "aRchiteutis")
ps <- kraken_to_phyloseq(path, pattern = "m11_.*k2", legend = legend,
                         trim_char = "_", rank = "G")
if (inherits(ps, "phyloseq")) phyloseq::ntaxa(ps) else nrow(ps$otu_table)

}
