% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_harness.R
\name{phyloseq_to_metacoder}
\alias{phyloseq_to_metacoder}
\title{Convert a phyloseq object to a metacoder Taxmap}
\usage{
phyloseq_to_metacoder(physeq, to_relative = TRUE)
}
\arguments{
\item{physeq}{A \code{phyloseq} object or an \code{archi_phyloseq} list.}

\item{to_relative}{Logical. Divide each sample by its total before parsing,
as the harness does.}
}
\value{
A \code{Taxmap}.
}
\description{
Same conversion as the harness \code{phyloseq2metacoder} skill. A real
\code{phyloseq} object goes through \link[metacoder:parse_phyloseq]{metacoder::parse_phyloseq} (that function
only sees \code{ranks_ref} after \pkg{metacoder} is attached). An
\code{archi_phyloseq} list, and any result that is still a forest, is parsed
from the rank table. Empty ranks are removed and children are reattached,
and a \code{root} node is added when the ranks do not already share one parent.
The returned graph is one tree.
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
legend <- system.file("extdata", "legend.csv", package = "aRchiteutis")
if (requireNamespace("metacoder", quietly = TRUE)) {
  ps <- kraken_to_phyloseq(path, pattern = "m1[12]_", legend = legend,
                           trim_char = "_", rank = "G")
  phyloseq_to_metacoder(ps)
}

}
