% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phyloseq.R
\name{ps2df}
\alias{ps2df}
\title{Convert a phyloseq object into the aRchiteutis long count table}
\usage{
ps2df(ps, ...)
}
\arguments{
\item{ps}{A \link[phyloseq:phyloseq]{phyloseq::phyloseq} object. Must contain an \code{otu_table} and a
\code{tax_table}; a \code{sample_data} slot is optional.}

\item{...}{Ignored, for forward compatibility.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} whose first six columns are, in order, \code{taxa}
(factor), \code{clade} (factor), \code{sample} (factor), \code{N} (numeric read count),
\code{amount} (\code{N} / total reads per sample) and \code{amount_cl} (\code{N} / classified
reads per sample). Any \code{sample_data} variables follow as columns 7+ and act
as the legend.
}
\description{
Converts a \link[phyloseq:phyloseq]{phyloseq::phyloseq} object into the long (tidy) \code{tibble} that the
rest of the package consumes, reproducing the hierarchical multi-clade
structure that \code{get_counts()} yields for Kraken2 reports. For \strong{every}
taxonomic rank present in the \code{tax_table} the counts are aggregated (summed)
per sample and emitted as their own rows, tagged with the matching Kraken
clade letter, and a per-sample root row (\code{clade == "R"}) is added holding the
total classified reads.
}
\details{
Ranks are matched case-insensitively and any subset of ranks is tolerated.
The rank -> clade-letter map is: Kingdom / Superkingdom / Domain -> \code{"D"},
Phylum -> \code{"P"}, Class -> \code{"C"}, Order -> \code{"O"}, Family -> \code{"F"},
Genus -> \code{"G"}, Species -> \code{"S"}. Empty / \code{NA} names at a given rank are
dropped for that rank. Because a standard \code{phyloseq} object records only
classified reads, the total and classified totals coincide, so \code{amount} and
\code{amount_cl} are equal.
}
\examples{
path <- system.file("extdata", package = "aRchiteutis")
df <- get_counts(path = path, pattern = "m1[124]_", trim_char = "_")
if (requireNamespace("phyloseq", quietly = TRUE)) {
  ps <- df2ps(df)          # build a phyloseq object from the example data
  df2 <- ps2df(ps)         # ... and convert it back
  head(df2)
}

}
\seealso{
\code{\link[=df2ps]{df2ps()}} for the reverse conversion and \code{\link[=get_counts]{get_counts()}} for the
native reader.
}
