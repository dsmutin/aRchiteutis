% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_phyloseq.R
\name{qza_to_phyloseq}
\alias{qza_to_phyloseq}
\title{Import QIIME 2 feature table, taxonomy and tree artifacts as phyloseq}
\usage{
qza_to_phyloseq(features, taxonomy = NULL, metadata = NULL, tree = NULL)
}
\arguments{
\item{features}{Feature-table \code{.qza}, an exported feature-table TSV, or a
directory of artifacts. A vector merges several runs after checking that
sample ids do not overlap.}

\item{taxonomy}{Taxonomy \code{.qza} or TSV. Discovered next to \code{features} when
that argument is a directory and this is \code{NULL}.}

\item{metadata}{Sample metadata TSV/CSV, or a \code{.qza}. Same discovery rule.}

\item{tree}{Rooted-tree \code{.qza} or Newick. Same discovery rule.}
}
\value{
A \code{phyloseq} object, or an \code{archi_phyloseq} list.
}
\description{
\code{features} may be a feature-table \code{.qza} or a directory that contains one.
Taxonomy and a rooted tree are optional \code{.qza} files. Metadata is a QIIME 2
manifest (TSV or CSV): a \code{sample-id} / \code{SampleID} column, or the first
column, plus any annotation columns such as \code{target}. When \pkg{qiime2R} is
installed it is tried first. Otherwise a feature-table TSV inside the
archive is read directly, and a BIOM table is read with \pkg{biomformat}.
Chloroplast, mitochondria and Chordata features are dropped. A Newick tree
is attached when its tips match the feature ids; otherwise a rank-formula
tree is built from the taxonomy table.
}
\examples{
qza <- system.file("extdata", "qza", package = "aRchiteutis")
if (requireNamespace("biomformat", quietly = TRUE)) {
  ps <- qza_to_phyloseq(qza)
  if (inherits(ps, "phyloseq")) phyloseq::nsamples(ps) else ncol(ps$otu_table)
}

}
