% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxonomy_tree.R
\name{ranks_to_tree}
\alias{ranks_to_tree}
\title{Build a taxonomy tree from a rank table}
\usage{
ranks_to_tree(lineage_df)
}
\arguments{
\item{lineage_df}{Data frame with one row per tip and columns among
\code{kingdom}, \code{phylum}, \code{class}, \code{order}, \code{family}, \code{genus}, \code{species}.
Optional \code{taxa_id}, \code{taxid}, and \code{tip_name}.}
}
\value{
An \link[ape:phylo]{ape::phylo} object.
}
\description{
Walks the seven Linnaean columns and keeps every rank as a node, including
unary parents. \code{ape::as.phylo} on a formula pastes those names into unquoted
Newick, so spaces in a species epithet split the tree and singleton ranks
collapse. Empty ranks still inherit the last classified name so a row always
has seven levels.
}
\examples{
lineage <- read.delim(
  system.file("extdata", "ncbi_lineage.tsv", package = "aRchiteutis"),
  stringsAsFactors = FALSE
)
tr <- ranks_to_tree(lineage)
tr$tip.label

}
